{
    "rewrites": [
      {"source": "/(.*)", "destination": "/index.html"}
    ],
    "buildCommand": "TAILWIND_DISABLE_OXIDE=1 npm run build",
    "outputDirectory": "dist",
    "installCommand": "npm install"
  }